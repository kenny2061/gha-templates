apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      codeType: go
      environment: dev

resources:
  - ../../base
replicas:
  - name: app-template
    count: 1
patches:
  - path: cpu-limit.yaml
  - path: ram-limit.yaml